@page
@model PRN221_MeVaBe_WebClient.Pages.Users.DetailsModel
@{
    ViewData["Title"] = "User Details";
}

<style>

    body {
        padding: 100px;
    }

    dt {
        font-weight: bold;
    }

    img {
        max-width: 200px;
        max-height: 200px;
    }
</style>


<head>
    <meta charset="utf-8">
    <title>Fruitables - Vegetable Website Template</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="" name="keywords">
    <meta content="" name="description">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&family=Raleway:wght@600;800&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="../lib/lightbox/css/lightbox.min.css" rel="stylesheet">
    <link href="../lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">


    <!-- Customized Bootstrap Stylesheet -->
    <link href="../css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="../css/style.css" rel="stylesheet">
</head>

<body>
    <div class="container">
        <h1 class="mt-5 mb-4 text-center">User Details</h1>

        <dl class="row">
            <dt class="col-sm-2">Fullname:</dt>
            <dd class="col-sm-10">@Model.User.Fullname</dd>

            <dt class="col-sm-2">Email:</dt>
            <dd class="col-sm-10">@Model.User.Email</dd>

            <dt class="col-sm-2">Phone:</dt>
            <dd class="col-sm-10">@Model.User.Phone</dd>

            <dt class="col-sm-2">Date of Birth:</dt>
            <dd class="col-sm-10">@Model.User.Dob?.ToString("yyyy-MM-dd")</dd>

            <dt class="col-sm-2">Avatar:</dt>
            <dd class="col-sm-10">
                <img src="@Model.User.Avatar" alt="Avatar" class="img-fluid rounded-circle">
            </dd>
        </dl>
    </div>

    <!-- Link tới Bootstrap JavaScript và các script tùy chỉnh của bạn nếu cần -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
